import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledOurServices = styled(Box)(({ theme }) => ({
  // Add any specific our services page styles here
}));
