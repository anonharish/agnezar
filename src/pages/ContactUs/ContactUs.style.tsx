import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledContactUs = styled(Box)(({ theme }) => ({
  // Add any specific contact us page styles here
}));
