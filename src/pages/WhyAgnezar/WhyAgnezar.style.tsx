import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledWhyAgnezar = styled(Box)(({ theme }) => ({
  // Add any specific why agnezar page styles here
}));
