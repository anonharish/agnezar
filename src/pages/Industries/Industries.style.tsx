import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledIndustries = styled(Box)(({ theme }) => ({
  // Add any specific industries page styles here
}));
